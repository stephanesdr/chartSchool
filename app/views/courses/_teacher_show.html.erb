<h1><%= @course.title %></h1>
<h2>Par <%= @course.creator.name %> </h2>
<p><%= @course.description %></p>
<p><%= @course.resource %></p>
<p><% @students.each do |student| %>
  <%= student.name %>
<%end%></p>

Ajouter un élève :
<%= form_tag url_for(course_attendances_path(@course.id)), multipart: true, method: "post" do %>



        <%= label_tag "Email de l'élève" %><br>
        <%= email_field_tag 'email', @email  %><br>

        <%= submit_tag "Inviter" %>

  <% end %>

Add steps :
<%= form_tag url_for(course_steps_path(@course.id)), multipart: true, method: "post" do %>



        <%= label_tag 'Titre' %><br>
        <%= text_field_tag 'title', @title  %><br>

        <%= label_tag 'Description' %><br>
        <%= text_area_tag 'description', @description  %><br>

        <%= submit_tag "Créer une étape" %>

  <% end %>

<% @steps.each do |step| %>
<%= step.title %>
<%= step.description %>
<br>
<%= step.percentage %>% des étudiants ont fini cette étape <br>

<%  step.percentage.to_i > 100 ? x = 1 : x = (step.percentage.to_f / 100).to_f %>
<%= progress_bar x , label: true, style: 'min-width: 20px' %>
<% end %>


<%= link_to("Créer un template à partir de ce cours", course_course_template_index_path(@course.id), method: :post) %>
