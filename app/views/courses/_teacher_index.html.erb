  <% if @pending_courses_as_teacher.present? %>
  <table style="width:100%">
    <tr>
      <th>Titre</th>
      <th>Date</th>
      <th>Link</th>
    </tr>
    <% @pending_courses_as_teacher.each do |course| %>
    <tr>
      <td><%= course.title %></td>
      <td><%= course.start_time %></td>
      <td><%= link_to 'lien', course_path(course) %></td>
    </tr>

    <% end %>
  <% else %>
    <p>Aucun cours archivé</p>
  <% end %>
  </table>
